<!DOCTYPE html>
<html>
<head lang="en">
	<meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Note</title>
	<link rel="stylesheet" type="text/css" href="./css/bootstrap.min.css">
	<link rel="stylesheet" type="text/css" href="./css/style.css">
	<link rel="stylesheet" type="text/css" href="./css/dark.css">
</head>
<body>
	<div class="content">
		<div class="container1">
			<a href='/newNote' class="btn btn-default">Create new note</a>
			<a href='/switchTheme' class="btn btn-default">Style Switcher</a>
		</div>
		<div class="container2">
			<div class="sortButtons">
				<a href='/?orderBy=finishDate' class="btn btn-default {{#if orderByFinishDate}}active{{/if}}">
					By finish Date {{#if orderByFinishDate}}{{#if orderAsc}}<span class="glyphicon glyphicon-triangle-top" aria-hidden="true"></span>{{else}}<span class="glyphicon glyphicon-triangle-bottom" aria-hidden="true"></span>{{/if}}{{/if}}
				</a>
				<a href='/?orderBy=createdDate' class="btn btn-default {{#if orderByCreatedDate}}active{{/if}}">
					By created Date {{#if orderByCreatedDate}}{{#if orderAsc}}<span class="glyphicon glyphicon-triangle-top" aria-hidden="true"></span>{{else}}<span class="glyphicon glyphicon-triangle-bottom" aria-hidden="true"></span>{{/if}}{{/if}}
				</a>
				<a href='/?orderBy=importance' class="btn btn-default {{#if orderByImportance}}active{{/if}}">
					By Importance {{#if orderByImportance}}{{#if orderAsc}}<span class="glyphicon glyphicon-triangle-top" aria-hidden="true"></span>{{else}}<span class="glyphicon glyphicon-triangle-bottom" aria-hidden="true"></span>{{/if}}{{/if}}
				</a>
			</div>
			<a href='/filterFinished' class="btn btn-default {{#if filterFinished}}active">Fertige anzeigen{{else}}">Fertige verbergen{{/if}}</a>
		</div>
		<div id="noteList" class="noteList">
			{{#if notes}}
				{{#each notes}}
				<div class="noteElement">
					<div class="upperContainer">

						<div class="due">{{#dueInDays dueTo}}{{/dueInDays}}</div>

						<div class="importance">
							{{#times priority}}
							<span class="glyphicon glyphicon-asterisk" aria-hidden="true"></span>
							{{/times}}
						</div>

						<div class="titleContainer">
							<div class="title">{{title}}</div>
						</div>

					</div>
					<div class="lowerContainer">

						<div class="finished">
							<input type="checkbox" id="finished" {{#if state}}checked{{/if}} disabled> Finished
						</div>

						<div class="desc">
							<textarea disabled>{{description}}</textarea>
						</div>

						<div class="btnEdit">
							<a href='./edit/{{_id}}' class="btn btn-default">Edit</a>
						</div>

					</div>
				</div>
				{{/each}}
			{{else}}
				<div class="bg-info info">Keine Todo gefunden</p>
			{{/if}}
		</div>
	</div>
    </div>
</body>
</html>